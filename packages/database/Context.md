# packages/database — Context

## Purpose
@pullquest/database — Supabase client factory, generated DB types, SQL migrations.

## Key Files
- `dist/`
- `node_modules/`
- `package.json`
- `src/`
- `supabase/`
- `tsconfig.json`

## Relationships
Consumed by API/worker (admin ops) and web (types); migrations applied via pnpm db:migrate or Supabase MCP.

## PRD Reference
§6.2 Supabase Layer
