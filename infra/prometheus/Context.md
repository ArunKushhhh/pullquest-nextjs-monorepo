# infra/prometheus — Context

## Purpose
Prometheus scrape config: API /metrics endpoint, self.

## Key Files
- `prometheus.yml`

## Relationships
host.docker.internal for host-run API; :9090 UI.

## PRD Reference
§8.4 Prometheus
